#include<stdio.h>
int main (){
	float total, desceunto=0, totalfinal;
	printf("total de compra: ");
	scanf("5f", &total); 
	if(total > 1000)
                 descuento = total * 0.10;
	totalfinal = total - descuento;
	printf("total final: %.2f", totalfinal);
	return 0;
}
 #include
 int main(){
 char prod